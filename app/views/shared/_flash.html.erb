<% flash.each do |type, message| %>
  <div class="mb-4 px-4 py-3 rounded-lg text-sm <%= type == 'notice' ? 'bg-teal-50 text-teal-700 border border-teal-200' : 'bg-red-50 text-red-700 border border-red-200' %>" data-turbo-temporary>
    <%= message %>
  </div>
<% end %>
